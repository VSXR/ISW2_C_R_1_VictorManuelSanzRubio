# https://docs.microsoft.com/azure/devops/pipelines/languages/python

trigger:
- master

variables:
  # Azure Resource Manager connection
  azureServiceConnectionId: '94edd487-1081-4af5-839e-e0c7d3865468'

  # Web app name
  webAppName: 'relecloud-web'

  # Agent VM image
  vmImageName: 'ubuntu-latest'

  # Environment name
  environmentName: 'relecloud-web'

  # IMPORTANTE: Definir la ruta de la subcarpeta donde está el código
  projectRoot: $(System.DefaultWorkingDirectory)/ISW2_C_R_1_VictorManuelSanzRubio

  # Updated Python version
  pythonVersion: '3.12'

stages:
- stage: Build
  displayName: Build stage
  jobs:
  - job: BuildJob
    pool:
      vmImage: $(vmImageName)
    steps:

    - task: UsePythonVersion@0
      inputs:
        versionSpec: '$(pythonVersion)'
      displayName: 'Use Python $(pythonVersion)'

    # Aquí usamos 'workingDirectory' para que los comandos se ejecuten dentro de la subcarpeta
    - script: |
        python -m venv antenv
        source antenv/bin/activate
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      workingDirectory: $(projectRoot)
      displayName: "Install requirements"

    - task: ArchiveFiles@2
      displayName: 'Archive files'
      inputs:
        # Archivamos SOLO el contenido de la subcarpeta del proyecto
        rootFolderOrFile: '$(projectRoot)'
        includeRootFolder: false  # IMPORTANTE: false para que manage.py quede en la raíz del ZIP
        archiveType: zip
        archiveFile: $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip
        replaceExistingArchive: true

    - upload: $(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip
      displayName: 'Upload package'
      artifact: drop

- stage: Deploy
  displayName: 'Deploy Web App'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - deployment: DeploymentJob
    pool:
      vmImage: $(vmImageName)
    environment: $(environmentName)
    strategy:
      runOnce:
        deploy:
          steps:

          - task: UsePythonVersion@0
            inputs:
              versionSpec: '$(pythonVersion)'
            displayName: 'Use Python version'

          - task: AzureWebApp@1
            displayName: 'Deploy Azure Web App : $(webAppName)'
            inputs:
              azureSubscription: $(azureServiceConnectionId)
              appName: $(webAppName)
              package: $(Pipeline.Workspace)/drop/$(Build.BuildId).zip
              # Opcional: Comando de inicio explícito si Azure no detecta la app automáticamente
              # startUpCommand: 'gunicorn --bind=0.0.0.0 --timeout 600 project.wsgi'
